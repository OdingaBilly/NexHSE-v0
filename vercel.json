{
  "buildCommand": "pnpm --filter @workspace/nexhse-africa run build && mkdir -p public && cp -r artifacts/nexhse-africa/dist/* public/",
  "outputDirectory": "public",
  "framework": "vite",
  "env": {
    "PORT": "5000",
    "BASE_PATH": "/"
  }
}
